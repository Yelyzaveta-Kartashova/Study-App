<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New topic for <%= @subject.name %></h1>

  <%= render "form", topic: @topic %>

  <%= link_to "Back to topics", subject_topics_path(@subject), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>

