<!DOCTYPE html>
<html>
  <head>
    <title>StudyApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body>
    <%= render "shared/navbar" %>
    
    <% if notice.present? %>
      <div class="notice bg-blue-100 border border-blue-300 text-blue-700 p-4 rounded mb-4 relative" role="alert">
        <%= notice %>
    <% end %>
    
    <% if alert.present? %>
      <div class="alert bg-red-100 border border-red-300 text-red-700 p-4 rounded mb-4 relative" role="alert">
        <%= alert %>
      </div>
    <% end %>

    <main class="container">
      <%= yield %>
    </main>
  </body>
</html>
