<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New lesson for <%= @topic.name %></h1>

  <%= render "form", lesson: @lesson %>

  <%= link_to "Back to topic", subject_topic_path(@subject, @topic), class: "mt-5 inline-block rounded-lg py-2 px-4 bg-gray-200 text-gray-800 font-medium shadow hover:bg-gray-300 transition-colors" %>
</div>

