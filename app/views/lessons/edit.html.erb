<div class="mx-auto mt-5 mb-5 max-w-3xl p-6 bg-gray-50 shadow rounded-lg overflow-auto min-h-6">
  <h1 class="font-bold text-4xl mb-6">Editing Lesson</h1>

  <%= render "form", lesson: @lesson %>

  <%= link_to "Back to Lessons", subject_topic_lessons_path(@subject, @topic, @lesson), 
      class: "mt-6 inline-block rounded-lg py-2 px-4 bg-gray-200 text-gray-800 font-medium shadow hover:bg-gray-300 transition-colors" %>
</div>
